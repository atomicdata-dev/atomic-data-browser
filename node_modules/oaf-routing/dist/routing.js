"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const oaf_side_effects_1 = require("oaf-side-effects");
// tslint:disable: no-object-mutation
// tslint:disable: no-expression-statement
// tslint:disable: no-if-statement
exports.handleFirstPageLoad = (settings, location, hash) => __awaiter(this, void 0, void 0, function* () {
    if (settings.setPageTitle) {
        document.title = yield settings.documentTitle(location);
    }
    if (settings.handleHashFragment) {
        setTimeout(() => {
            const focusTarget = oaf_side_effects_1.elementFromHash(hash);
            if (focusTarget !== undefined) {
                oaf_side_effects_1.focusAndScrollIntoViewIfRequired(focusTarget, focusTarget, settings.focusOptions, settings.scrollIntoViewOptions);
            }
        }, settings.renderTimeout);
    }
});
