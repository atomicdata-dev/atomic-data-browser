import { History, Location } from "history";
import { RouterSettings } from "oaf-routing";
export { RouterSettings } from "oaf-routing";
export declare const defaultSettings: RouterSettings<Location<unknown>>;
export declare const wrapHistory: <A = unknown>(history: History<A>, settingsOverrides?: Partial<RouterSettings<Location<A>>> | undefined) => () => void;
